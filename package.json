{
  "name": "boilerplate",
  "version": "1.0.0",
  "description": "boilderplate project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack && node-sass ./scss -o ./css && exit 0",
    "watch": "webpack --watch & node-sass --watch ./scss -o ./css",
    "serve": "npm run watch & live-server"
  },
  "author": "Derrick Henry",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.25.0",
    "babel-loader": "^7.1.1",
    "babel-preset-latest": "^6.24.1",
    "node-sass": "^4.5.3",
    "webpack": "^3.1.0"
  },
  "dependencies": {
    "jquery": "^3.2.1"
  }
}
